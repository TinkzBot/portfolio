{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="landingpage" role="main">
        {{ content }}

        {% for page in pages("technologies") %}
            <a class="objekt" href="{{ page.url }}">
            <div class="technology">
                {{ page.title }}
            </div>
            </a>
        {% endfor %}
        </div>

    {% include 'incl/footer.twig' %}
</body>

</html>